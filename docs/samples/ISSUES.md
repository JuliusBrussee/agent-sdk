# Sample-project issue ledger

This ledger records defects found by building and operating repository samples.
An issue closes only with root-cause fix and regression proof. Environment and
credential blockers stay distinct from product defects.

| ID | Severity | Status | Finding | Evidence | Resolution / next proof |
| --- | --- | --- | --- | --- | --- |
| SAMPLE-001 | P0 | fixed | Checkout `origin` targeted `JuliusBrussee/agent-sdk-ollldd`, while repository contract names `JuliusBrussee/agent-sdk`. Root build/test/pack correctly failed closed. | Original `git remote -v`; `npm run repository:check` contract in root scripts | Fetch/push URL now canonical. `npm run repository:setup` installed local pre-push guard; `npm run repository:check` passes. Identity and hook regression tests also pass. |
| SAMPLE-002 | P1 | open | Five adapter READMEs show imports only. None provides complete identity, Cave Build, request, framework construction, execution, or receipt path. A consumer cannot run an adapter from published documentation. | `packages/adapters/*/README.md`; adapter constructors require `HarnessAdapterIdentity` plus `HarnessRequest` or framework binding | Build exact adapter lanes, then replace snippets with links and minimal complete paths. Any repeated low-level ceremony becomes SDK helper only if contract stays fail closed. |
| SAMPLE-003 | P1 | fixed | Root example coverage previously contained one coding-agent wrapper with two structural assertions. No business workflow, HTTP boundary, tenant isolation, durable receipt, negative-path execution, or adapter example existed. | Root sample discovery now covers standalone enterprise and adapter projects; samples contain no action-gate state. | Keep sample discovery fail-closed and require every added sample package to execute through root `test:example`. |
| SAMPLE-004 | P1 | open | Root package declares Node `>=22.19.0`, while Eve adapter requires Node `>=24.0.0`. One undifferentiated sample command would either fail on supported root Node 22 or silently skip Eve. | root and Eve `package.json` engines | Sample runner must classify Eve as Node-24-required, fail only its requested live lane on Node 22, and run all other lanes. CI matrix needs explicit Node 22 and Node 24 jobs before Eve proof can close. |
| SAMPLE-005 | P1 | fixed | Public `schema` helper could not express JSON `null`, forcing structured enterprise outputs to weaken nullable fields to `any` or bypass SDK schema validation. Underlying TypeBox runtime already supports null. | Support proposal needs nullable refund and escalation fields; TypeScript reported `Property 'null' does not exist` | Added `schema.null()` backed by `Type.Null()` and type-level structured-output regression. Support sample now keeps nullable fields explicit. Full Agent SDK test remains required. |
| SAMPLE-006 | P0 | fixed | Public embedded `run()` rejected caller-supplied Cave Build and exposed no supported locked runner. Mini-SaaS could not serve optimized traffic. | Original `RunOptions` blocked `lockedBuild`; CLI private runner was only path | Added public `runLocked()`. It parses lock, binds agent/runtime/adapter/upstream/catalog/Context IR/plan and live registry before provider traffic. Support startup runs `caveman-agent check` for source/eval/config freshness before loading lock. Stale adapter regression proves zero provider calls. |
| SAMPLE-007 | P0 | fixed | Durable execution rejected locked build. Flagship support workflow had to choose crash recovery or compiled optimization. | Original durable admission threw `cave_durable_locked_build_unsupported` | Locked build/plan digests now join durable definition identity. Lock validates before replay; exact terminal replay spends once; candidate plans remain rejected. Public regression proves optimized durable replay with one provider call. |
| SAMPLE-008 | P1 | open | Native Pi compiler refuses every root tool, including deterministic read-only policy lookup. Tool-using support bot cannot produce behaviorally optimized native build. | compiler docs and capability gate; tool presence rejects before search spend | Move tenant/order/policy retrieval into application-owned evidence for flagship tool-free compiled lane, while preserving separate tool workflow samples. Track read-only native-tool semantics as SDK capability gap; never pretend generic baseline lock is optimized. |
| SAMPLE-009 | P2 | fixed | Bundled `ai@7.0.43` README constructs `ToolLoopAgent` with `system`, but installed public type `ToolLoopAgentSettings` accepts `instructions`. Copying prose example fails TypeScript. | Vercel adapter sample typecheck: `'system' does not exist in type 'ToolLoopAgentSettings'`; installed declaration names `instructions` | Sample uses executable `instructions` contract. Exact version typecheck guards future drift. Upstream documentation issue remains outside this repository. |
| SAMPLE-010 | P1 | fixed | CLI could hash NodeNext `.js` specifiers to authored `.ts`, but dev/build import executed them without runtime extension mapping. Normal multi-file TypeScript agent failed before static checks. Hoisted workspace installs also failed because staging required project-local `node_modules`. | Support doctor failed on `src/agent.ts` importing `./domain.js`, then on missing sample-local `node_modules` | Dev staging now creates transformed runtime aliases from authored `.ts/.mts/.cts`, preserves original source graph identity, and resolves nearest hoisted `node_modules`. Regression executes typed helper through `.js` specifier from nested workspace. |
| SAMPLE-011 | P1 | fixed | Source-lock scanner treated every computed `new URL(value, base)` as file loading, including HTTP request parsing. Production server source could not enter build graph. | Support doctor rejected `new URL(request.url ?? "/", "http://support.local")` in `src/server.ts` | Scanner now rejects computed path only when base is `import.meta.url`; ordinary runtime URL parsing is ignored. Computed file-dependency and ordinary-URL regressions both pass. |
| SAMPLE-012 | P1 | environment | Local `caveman` CLI and gateway exist, but `caveman-engine` binary and provider credential are absent. Real support compile and same-case off/on spend cannot run in this checkout yet. | Escalated doctor: sandbox/project/gateway pass; engine warns `spawn caveman-engine ENOENT`; lock/provider warn absent | Run `caveman setup`, authenticate provider and Caveman account, then `npm run optimize`, `npm run check:optimization`, and `npm run compare -- --yes`. No optimized claim until those gates produce receipts. |
