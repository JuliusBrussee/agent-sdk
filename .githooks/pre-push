#!/bin/sh
set -eu

remote_url=${2:-}
if [ -z "$remote_url" ]; then
  echo "repository_identity_push_url_missing" >&2
  exit 1
fi

repository_root=$(git rev-parse --show-toplevel)
exec node "$repository_root/scripts/check-repository-identity.mjs" --push-url "$remote_url"
